import 'package:flutter/material.dart';

Widget profilPage(){
  return ListView(
    children: [
      Padding(
        padding: EdgeInsets.only(top: 40),
        child: Image.asset(
          'assets/image/Perguruan.png',
          height: 113,
        ),
      ),
    ],
  );
}
