import 'package:flutter/material.dart';


Widget profilPage(){
  return ListView(
    children: [
      Container(
        width: double.infinity,
        height: 50,
        color: Colors.black,
      ),
      Container(
        width: double.infinity,
        height: 50,
        color: Colors.blue,
      ),
    ],
  );
}
